// script.js
/*
// Toggle dark mode
//document.getElementById('toggle-dark-mode').addEventListener('click', function () {
  // This function is called when the "toggle-dark-mode" element is clicked.
  // It toggles the "dark" class on the document's root element,
  // which is used to apply a dark theme to the page.
  //document.documentElement.classList.toggle('dark');
});

// Open and close modal
function openModal(eventName) {
  // This function is called to open the modal.
  // It removes the "hidden" class from the modal element, making it visible.
  // It also sets the event name in the modal's title.
  document.getElementById('modal').classList.remove('hidden');
  document.getElementById('modal-event-name').textContent = eventName;
}

function closeModal() {
  // This function is called to close the modal.
  // It adds the "hidden" class back to the modal element, making it invisible.
  document.getElementById('modal').classList.add('hidden');
}

*/

